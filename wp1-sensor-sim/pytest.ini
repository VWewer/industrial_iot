[pytest]
asyncio_mode = strict

markers =
    integration: marks tests that require a running MQTT broker (deselect with -m "not integration")

filterwarnings =
    ignore::starlette.exceptions.StarletteDeprecationWarning
